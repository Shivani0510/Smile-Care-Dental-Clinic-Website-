// Central place to configure where the frontend finds the API.
// If you serve the frontend from the same Express app (as server.js does by
// default), a relative path works everywhere with no changes needed.
// If you deploy the frontend separately (e.g. GitHub Pages) point this at
// your backend's deployed URL, e.g. 'https://your-api.onrender.com/api'.
const API_BASE_URL = '/api';
